RPC remote procedure call
http 请求则是使用 http协议实现的 rpc
而 rpc还可以使用其他通信协议来实现

由于 http的请求报文中 包含了太多“废消息”，所以在请求量较大时效率偏低
而使用自定义的tcp协议则可以在很大程度上减轻请求中携带数据重量。

http好比普通话，rpc好比团伙内部黑话。

讲普通话，好处就是谁都听得懂，谁都会讲。

讲黑话，好处是可以更精简、更加保密、更加可定制，
坏处就是要求“说”黑话的那一方（client端）也要懂，而且一旦大家都说一种黑话了，换黑话就困难了。