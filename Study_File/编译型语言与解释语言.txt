编译语言指的是可以直接将源代码编译成二进制机器代码，之后机器可以直接执行，例如 C/C++都是
编译语言，所以执行效率相对来说较高，但是跨平台性差，因为在不同的平台，编译出来的机器代码不相同

而解释型语言 则是在运行阶段 通过解释器来达到 边解释边运行的效果，例如 java script 所以运行效率不高
java 是特殊的解释型语言，首先他也要进行编译，但产物不是机器代码，而是 字节码，能够被 java平台的 jvm
虚拟机所运行，之后 jvm 再将字节码解释成 机器代码，所以 java可以跨平台。